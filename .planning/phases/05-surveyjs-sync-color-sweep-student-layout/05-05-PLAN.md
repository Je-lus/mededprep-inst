---
phase: 05-surveyjs-sync-color-sweep-student-layout
plan: 05
type: execute
wave: 3
depends_on:
  - 05-03
  - 05-04
files_modified: []
autonomous: false
requirements:
  - QAL-01
  - QAL-02
  - QAL-03
  - QAL-04
  - QAL-05

must_haves:
  truths:
    - 'npm run build passes clean with zero errors'
    - 'npm run typecheck passes clean with zero errors'
    - 'Admin dashboard, assessment creation, and take-assessment flow render correctly in both themes'
    - 'Mobile sidebar collapses to sheet and all pages are responsive'
    - 'No white flashes on page load or navigation in Glass Purple'
  artifacts: []
  key_links: []
---

<objective>
Run the final quality gate: build verification, TypeScript check, and visual/mobile testing across both themes to confirm Phase 5 is complete.

Purpose: Phase 5 touches 36+ files with mechanical replacements. A clean build and typecheck confirms no typos or broken imports. Visual verification confirms the semantic tokens resolve correctly in both themes.
Output: Confirmed passing build, typecheck, and visual/functional checks across both themes on desktop and mobile.
</objective>

<execution_context>
@/home/jeramey/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeramey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-03-SUMMARY.md
@.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and typecheck quality gates</name>
  <files></files>
  <action>
1. Run TypeScript check:
```bash
cd app && npm run typecheck
```
Must pass with zero errors. If errors exist, fix them before proceeding.

2. Run production build:

```bash
cd app && npm run build
```

Must complete successfully with zero errors. Warnings are acceptable but errors are blockers.

3. Run the final color sweep grep verification one more time to confirm:

```bash
grep -rn "bg-white\|bg-gray-\|text-gray-\|border-gray-\|text-slate-\|border-slate-\|primary-50[^0]\|primary-100\|primary-500" app/src/ --include="*.tsx" --include="*.ts" | grep -v "intentional" | grep -v "node_modules" | grep -v "__tests__" | grep -v "\.d\.ts"
```

Must return zero lines.

4. Verify the .sd-root-modern border-color revert block is still in index.css:

```bash
grep "border-color: revert" app/src/index.css
```

Must return the revert rule.

If any step fails, diagnose and fix before marking complete.
</action>
<verify>
<automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst/app && npm run typecheck && npm run build</automated>
</verify>
<done>TypeScript check passes clean. Production build passes clean. Final grep returns zero non-intentional forbidden patterns. SurveyJS border-color revert block intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and mobile verification across both themes</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for approval. Phase 5 is complete: SurveyJS theme sync, student layout upgrade, and full semantic color sweep across 36+ files. All hardcoded gray/white/slate/primary-500 classes replaced with semantic tokens. Intentional whites preserved with comments.

**Desktop verification (QAL-03):**

1. Start the dev server: `npm run dev` (backend) + `cd app && npm run dev` (frontend)
2. Open http://localhost:9000 in the browser
3. Log in as admin (admin@demo.org / password123)
4. **Daylight theme:** Navigate to Dashboard, Assessments, create an assessment, view assessment detail tabs — verify everything looks normal (no regressions)
5. Switch to Glass Purple via the toggle
6. **Glass Purple:** Re-check Dashboard, Assessments, assessment detail — verify dark backgrounds, readable text, no white patches (except QR codes)
7. Take an assessment (SurveyJS player) in Glass Purple — verify dark survey background, purple buttons, light text
8. Take the same assessment in Daylight — verify it looks like the original
9. Navigate to student pages — verify Glass Purple background effects and working toggle
10. Check Bug Reports page — verify severity badges are readable in both themes

**Mobile verification (QAL-04):** 11. Open DevTools, toggle mobile viewport (375px width) 12. In Glass Purple: verify sidebar collapses to sheet/hamburger, all pages responsive 13. In Daylight: verify same responsive behavior

**FOUC verification (QAL-05):** 14. In Glass Purple theme, hard-refresh the page (Ctrl+Shift+R) — verify NO white flash before the dark theme loads 15. Navigate between pages in Glass Purple — verify no white flashes on route transitions

Resume signal: Type "approved" if everything looks correct, or describe any visual issues found.
</action>
<verify>
<automated>echo "Checkpoint: awaiting human verification"</automated>
<manual>Human visually verifies all 15 checklist items above across both themes on desktop and mobile</manual>
</verify>
<done>Human has approved visual verification: both themes render correctly on desktop and mobile, no white flashes, SurveyJS themed correctly, all pages responsive.</done>
</task>

</tasks>

<verification>
- `npm run build` passes clean
- `npm run typecheck` passes clean
- Visual verification confirms both themes render correctly on desktop and mobile
- No white flashes in Glass Purple on load or navigation
- SurveyJS player themed correctly in both modes
</verification>

<success_criteria>

- Build and typecheck pass with zero errors (QAL-01, QAL-02)
- Admin dashboard, assessment creation, take-assessment flow render correctly in both themes (QAL-03)
- Mobile sidebar collapses, all pages responsive (QAL-04)
- No white flashes on page load or navigation in Glass Purple (QAL-05)
- Human verification approved
  </success_criteria>

<output>
After completion, create `.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-05-SUMMARY.md`
</output>
