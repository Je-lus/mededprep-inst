---
phase: 05-surveyjs-sync-color-sweep-student-layout
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - app/src/pages/admin/AssessmentList.tsx
  - app/src/pages/admin/AssessmentDetail.tsx
  - app/src/pages/admin/AssessmentCreate.tsx
  - app/src/pages/admin/BugReports.tsx
  - app/src/pages/admin/SessionList.tsx
  - app/src/pages/admin/SessionDetail.tsx
  - app/src/pages/admin/QuestionBankList.tsx
  - app/src/pages/admin/QuestionBankCreate.tsx
  - app/src/pages/admin/QuestionBankDetail.tsx
  - app/src/pages/admin/InstructorList.tsx
  - app/src/pages/admin/QrPresenter.tsx
  - app/src/pages/admin/assessment-detail/EditAssessmentDialog.tsx
  - app/src/pages/admin/assessment-detail/ItemAnalysisTab.tsx
  - app/src/pages/admin/assessment-detail/ResponsesTab.tsx
  - app/src/pages/admin/assessment-detail/QrCodeTab.tsx
  - app/src/pages/student/StudentLogin.tsx
  - app/src/pages/student/ForgotPassword.tsx
  - app/src/pages/student/ResetPassword.tsx
  - app/src/pages/student/StudentDashboard.tsx
  - app/src/pages/student/AssessmentReview.tsx
autonomous: true
requirements:
  - CLR-01
  - CLR-02
  - CLR-03
  - CLR-04
  - CLR-08
  - CLR-09

must_haves:
  truths:
    - 'All admin pages render correctly in both themes with no hardcoded gray or white surviving'
    - 'All student pages use semantic color tokens'
    - 'QR code backgrounds in QrCodeTab and QrPresenter remain white (intentional)'
    - 'QrPresenter card with forced light-on-dark styling is preserved with intentional comments'
    - 'Post-sweep grep confirms zero remaining forbidden pattern matches in source files'
  artifacts:
    - path: 'app/src/pages/admin/QrPresenter.tsx'
      provides: 'Presenter mode with preserved intentional whites and slates'
      contains: 'intentional'
    - path: 'app/src/pages/admin/assessment-detail/QrCodeTab.tsx'
      provides: 'QR code tab with preserved intentional white QR background'
      contains: 'intentional'
  key_links:
    - from: 'app/src/pages/admin/assessment-detail/ItemAnalysisTab.tsx'
      to: 'Tailwind semantic tokens'
      via: 'className strings'
      pattern: 'bg-card.*border-border'
---

<objective>
Complete the color sweep across all admin pages and student pages, preserve intentional whites with comments, and run the final grep verification to confirm zero forbidden patterns remain.

Purpose: The remaining ~20 page files still use hardcoded gray, white, slate, and primary-500 classes that break in Glass Purple. This plan completes the sweep and runs the final verification grep.
Output: All source files using semantic tokens, intentional whites documented, grep verification passing.
</objective>

<execution_context>
@/home/jeramey/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeramey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color sweep - admin pages</name>
  <files>
    app/src/pages/admin/AssessmentList.tsx
    app/src/pages/admin/AssessmentDetail.tsx
    app/src/pages/admin/AssessmentCreate.tsx
    app/src/pages/admin/BugReports.tsx
    app/src/pages/admin/SessionList.tsx
    app/src/pages/admin/SessionDetail.tsx
    app/src/pages/admin/QuestionBankList.tsx
    app/src/pages/admin/QuestionBankCreate.tsx
    app/src/pages/admin/QuestionBankDetail.tsx
    app/src/pages/admin/InstructorList.tsx
    app/src/pages/admin/QrPresenter.tsx
    app/src/pages/admin/assessment-detail/EditAssessmentDialog.tsx
    app/src/pages/admin/assessment-detail/ItemAnalysisTab.tsx
    app/src/pages/admin/assessment-detail/ResponsesTab.tsx
    app/src/pages/admin/assessment-detail/QrCodeTab.tsx
  </files>
  <action>
Apply the semantic color replacement map from research. For each file:

**AssessmentList.tsx:** `bg-primary-500` (2 hits) -> `bg-primary`

**AssessmentDetail.tsx:** `bg-primary-500` (2 hits) -> `bg-primary`

**AssessmentCreate.tsx:** `bg-primary-500` (4 hits) -> `bg-primary`

**BugReports.tsx (CLR-02):**

- `bg-gray-400 text-white` on low-severity badge -> `bg-muted text-muted-foreground`
- `bg-gray-50` page background -> `bg-background`
- Do NOT touch `bg-red-100`, `bg-blue-100`, `bg-purple-100`, `bg-orange-500`, `bg-yellow-500` — these are intentional severity/category colors

**SessionList.tsx:**

- `bg-primary-500` -> `bg-primary`
- `bg-gray-100 text-gray-600` state badge -> `bg-muted text-muted-foreground`

**SessionDetail.tsx:**

- `bg-primary-500` -> `bg-primary`
- `bg-gray-100 text-gray-600` state badge -> `bg-muted text-muted-foreground`

**QuestionBankList.tsx:** `bg-primary-500` (2 hits) -> `bg-primary`

**QuestionBankCreate.tsx:** `bg-primary-500` -> `bg-primary`

**QuestionBankDetail.tsx:** `bg-primary-500` (5 hits) -> `bg-primary`

**InstructorList.tsx:** `bg-primary-500` (2 hits) -> `bg-primary`

**QrPresenter.tsx (CLR-08 — SPECIAL CASE):**

- `text-primary-500` -> `text-primary` (this is the only non-intentional reference)
- ALL `bg-white`, `text-slate-900`, `bg-slate-950`, `bg-slate-100`, `text-slate-*`, `border-slate-300` references are INTENTIONAL — presenter mode renders a white card on a near-black background for classroom projector readability
- Add `/* intentional: presenter mode — forced light card on dark backdrop for projector readability */` comment at the top of the presenter card JSX block
- Mark each intentional slate/white usage with inline `/* intentional: presenter mode */` comment

**EditAssessmentDialog.tsx:** `bg-primary-500` -> `bg-primary`

**ItemAnalysisTab.tsx (CLR-01, CLR-04):**

- `border-gray-200 bg-white` choice container -> `border-border bg-card`
- `bg-gray-300` bar -> `bg-muted`
- Card `bg-white` -> remove explicit `bg-white` if the Card component already provides `bg-card` via semantic token (check if `<Card>` has its own background). If Card does not have a default bg, use `bg-card`

**ResponsesTab.tsx (CLR-02):**

- `bg-gray-200` progress bar background -> `bg-muted`

**QrCodeTab.tsx (CLR-08):**

- `bg-white p-2` on QR image container -> PRESERVE with comment: `/* intentional: QR code readability requires white background */`
- `bg-primary-500` on button -> `bg-primary`

AVOID:

- Touching any non-color classes
- Replacing intentional colored status badges (red, blue, purple, orange, yellow)
- Removing the QrPresenter `bg-slate-950` background (intentional dark backdrop)
- Replacing `bg-white` on QR code image containers
  </action>
  <verify>
  <automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst && grep -rn "bg-primary-500\|primary-50[^0]\|primary-100\|primary-500\|bg-gray-\|text-gray-\|border-gray-" app/src/pages/admin/ 2>/dev/null | grep -v "intentional" | head -20</automated>
  <manual>Spot-check 3-4 admin pages in browser in both themes</manual>
  </verify>
  <done>All 15 admin page files use semantic Tailwind tokens. QrPresenter and QrCodeTab intentional whites/slates are preserved with comments. No remaining forbidden patterns in admin pages (except intentional-marked).</done>
  </task>

<task type="auto">
  <name>Task 2: Color sweep - student pages + final grep verification</name>
  <files>
    app/src/pages/student/StudentLogin.tsx
    app/src/pages/student/ForgotPassword.tsx
    app/src/pages/student/ResetPassword.tsx
    app/src/pages/student/StudentDashboard.tsx
    app/src/pages/student/AssessmentReview.tsx
  </files>
  <action>
**StudentLogin.tsx (CLR-03):**
- `text-gray-600` (3 hits) -> `text-muted-foreground`

**ForgotPassword.tsx (CLR-03):**

- `text-gray-600` -> `text-muted-foreground`

**ResetPassword.tsx (CLR-03):**

- `text-gray-600` (2 hits) -> `text-muted-foreground`

**StudentDashboard.tsx (CLR-03):**

- `text-gray-600` -> `text-muted-foreground`
- Do NOT touch `bg-blue-50`, `bg-amber-50`, `bg-emerald-50` — intentional semantic status colors for stats cards

**AssessmentReview.tsx (CLR-03, CLR-04):**

- `text-gray-600` -> `text-muted-foreground`
- `border-slate-200 bg-white text-slate-700` on answer option buttons -> `border-border bg-card text-foreground`
- `bg-slate-100` progress bar -> `bg-muted`
- `bg-slate-400` used progress fill -> `bg-muted-foreground`
- Do NOT touch `bg-emerald-50`, `bg-red-50`, `text-emerald-*`, `text-red-*` — these are answer correctness indicators

**FINAL VERIFICATION (CLR-09):**
After all student page replacements, run the comprehensive grep across ALL source files to verify zero remaining forbidden patterns:

```bash
grep -rn "bg-white\|bg-gray-\|text-gray-\|border-gray-\|text-slate-\|border-slate-\|primary-50\b\|primary-100\b\|primary-500\b" app/src/ --include="*.tsx" --include="*.ts" | grep -v "intentional" | grep -v "node_modules" | grep -v "__tests__"
```

Every line returned must be either:

1. An intentional preservation (already marked with `/* intentional */` comment)
2. A false positive (e.g., `primary-500` appearing in a string constant, not a class name)

If any legitimate forbidden patterns remain, fix them in the same task.

AVOID:

- Replacing status/correctness colors (emerald, red, blue, amber)
- Replacing any line already marked with `/* intentional */`
  </action>
  <verify>
  <automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst && grep -rn "bg-white\|bg-gray-\|text-gray-\|border-gray-\|text-slate-\|border-slate-\|primary-50[^0]\|primary-100\|primary-500" app/src/ --include="_.tsx" --include="_.ts" | grep -v "intentional" | grep -v "node_modules" | grep -v "**tests**" | grep -v "\.d\.ts" | head -20</automated>
  <manual>Verify the grep returns zero non-intentional results</manual>
  </verify>
  <done>All 5 student page files use semantic Tailwind tokens. Final grep across all 36+ source files confirms zero remaining forbidden patterns (bg-white, bg-gray-_, text-gray-_, border-gray-_, text-slate-_, border-slate-\*, primary-50, primary-100, primary-500) except intentional-marked lines.</done>
  </task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes with zero errors
- Full-codebase grep for forbidden patterns returns only intentional-marked lines
- QrPresenter intentional whites/slates have comments
- QrCodeTab QR image bg-white has intentional comment
- AssessmentReview answer options use border-border bg-card text-foreground
</verification>

<success_criteria>

- All 20 files migrated to semantic tokens
- CLR-09 grep verification passes: zero non-intentional forbidden patterns remain
- All intentional whites documented with inline comments
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-04-SUMMARY.md`
</output>
