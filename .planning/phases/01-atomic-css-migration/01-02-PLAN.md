---
phase: 01-atomic-css-migration
plan: 02
type: execute
wave: 2
depends_on:
  - '01-01'
files_modified:
  - app/src/index.css
  - app/tailwind.config.js
  - app/src/components/AdminLayout.tsx
  - app/src/components/BugReportButton.tsx
  - app/src/components/BugReportDialog.tsx
  - app/src/components/StatusBadge.tsx
  - app/src/components/ToggleSwitch.tsx
  - app/src/pages/Dashboard.tsx
  - app/src/pages/Welcome.tsx
  - app/src/pages/admin/AssessmentCreate.tsx
  - app/src/pages/admin/AssessmentDetail.tsx
  - app/src/pages/admin/AssessmentList.tsx
  - app/src/pages/admin/InstructorList.tsx
  - app/src/pages/admin/QrPresenter.tsx
  - app/src/pages/admin/QuestionBankCreate.tsx
  - app/src/pages/admin/QuestionBankDetail.tsx
  - app/src/pages/admin/QuestionBankList.tsx
  - app/src/pages/admin/SessionDetail.tsx
  - app/src/pages/admin/SessionList.tsx
  - app/src/pages/admin/assessment-detail/EditAssessmentDialog.tsx
  - app/src/pages/admin/assessment-detail/QrCodeTab.tsx
  - app/src/pages/public/take-assessment/StudentInfoStep.tsx
autonomous: true
requirements:
  - CSS-01
  - CSS-02
  - CSS-03
  - CSS-04
  - CSS-05
  - CSS-06
must_haves:
  truths:
    - 'The app renders visually identical to the pre-migration state — no colors, borders, or shadows have changed in Daylight mode'
    - '`var(--primary)` resolves to `#1b5fd0` in DevTools with no hsl() wrapper anywhere in index.css or tailwind.config.js'
    - 'Searching source files for `hsl(var(--` returns zero results'
    - 'Searching source files for `primary-500`, `primary-100`, `primary-50`, `primary-600`, `primary-700` returns zero results'
    - '`npm run build` and `npm run typecheck` pass with zero errors'
    - "Glass Purple CSS variables are defined under `[data-theme='glass']` selector"
  artifacts:
    - path: 'app/src/index.css'
      provides: "Hex CSS variables under :root, Glass Purple variables under [data-theme='glass'], border-color using var(--border)"
      contains: '--primary: #1b5fd0'
    - path: 'app/tailwind.config.js'
      provides: 'var(--xxx) color tokens with no hsl() wrapper, no primary-50/100/500/600/700 entries'
      contains: 'var(--primary)'
    - path: 'app/src/components/AdminLayout.tsx'
      provides: 'Semantic class replacements for active nav styling'
      contains: 'border-primary'
  key_links:
    - from: 'app/src/index.css'
      to: 'app/tailwind.config.js'
      via: 'CSS variables consumed by Tailwind color tokens'
      pattern: "var\\(--primary\\)"
    - from: 'app/tailwind.config.js'
      to: 'app/src/components/*.tsx'
      via: 'Tailwind utility classes in component className attributes'
      pattern: 'bg-primary|text-primary|border-primary'
    - from: 'app/src/index.css'
      to: 'app/src/index.css'
      via: 'border-color rule references --border variable'
      pattern: "border-color: var\\(--border\\)"
---

<objective>
Perform the complete atomic CSS migration: convert all CSS variables from HSL-component format to raw hex, switch Tailwind config from `hsl(var(--xxx))` to `var(--xxx)`, replace all 47 `primary-NNN` class references across 20 component files with semantic equivalents, add Glass Purple theme variables, and commit everything as a single atomic unit.

Purpose: This is the foundational migration that enables all subsequent theme work. The app must render identically after this change (Daylight mode), while gaining a clean CSS variable system that the `[data-theme="glass"]` selector can override in Phase 2.

Output: One atomic commit containing all 22 file changes. The app looks identical but the CSS variable infrastructure is migrated.
</objective>

<execution_context>
@/home/jeramey/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeramey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-atomic-css-migration/01-RESEARCH.md
@.planning/phases/01-atomic-css-migration/01-01-SUMMARY.md
@app/src/index.css
@app/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CSS variables and Tailwind config</name>
  <files>app/src/index.css, app/tailwind.config.js</files>
  <action>
**CRITICAL: Do NOT commit after this task. Tasks 1 and 2 MUST be committed together as a single atomic commit after Task 2 completes. Any intermediate state breaks every semantic color in the app.**

**1. Replace `app/src/index.css` entirely with the following content:**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: #ffffff;
    --foreground: #020817;
    --card: #ffffff;
    --card-foreground: #020817;
    --popover: #ffffff;
    --popover-foreground: #020817;
    --primary: #1b5fd0;
    --primary-foreground: #f8fafc;
    --secondary: #f1f5f9;
    --secondary-foreground: #0f172a;
    --muted: #f1f5f9;
    --muted-foreground: #64748b;
    --accent: #f1f5f9;
    --accent-foreground: #0f172a;
    --destructive: #ef4444;
    --destructive-foreground: #f8fafc;
    --border: #e2e8f0;
    --input: #e2e8f0;
    --ring: #1b5fd0;
    --radius: 0.5rem;
    --chart-1: #e76e50;
    --chart-2: #2a9d90;
    --chart-3: #274754;
    --chart-4: #e8c468;
    --chart-5: #f4a462;
  }

  [data-theme='glass'] {
    --background: #0d0a1e;
    --foreground: #f8fafc;
    --card: rgba(17, 12, 36, 0.7);
    --card-foreground: #f8fafc;
    --popover: rgba(17, 12, 36, 0.95);
    --popover-foreground: #f8fafc;
    --primary: #8b5cf6;
    --primary-foreground: #ffffff;
    --secondary: rgba(139, 92, 246, 0.1);
    --secondary-foreground: #e2e8f0;
    --muted: rgba(139, 92, 246, 0.05);
    --muted-foreground: #94a3b8;
    --accent: rgba(139, 92, 246, 0.15);
    --accent-foreground: #f8fafc;
    --destructive: #ef4444;
    --destructive-foreground: #ffffff;
    --border: rgba(139, 92, 246, 0.2);
    --input: rgba(139, 92, 246, 0.1);
    --ring: #8b5cf6;
    --chart-1: #a78bfa;
    --chart-2: #34d399;
    --chart-3: #60a5fa;
    --chart-4: #fbbf24;
    --chart-5: #f87171;
  }
}

@layer base {
  * {
    border-color: var(--border);
  }
  body {
    @apply bg-background text-foreground;
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', sans-serif;
    -webkit-font-smoothing: antialiased;
  }
}

/* Prevent Tailwind base border-color from overriding SurveyJS internal styles */
.sd-root-modern *,
.sd-root-modern *::before,
.sd-root-modern *::after {
  border-color: revert;
}
```

Key changes from original:

- `:root` variables now store raw hex values (CSS-06)
- `[data-theme='glass']` block added with Glass Purple palette (CSS-05)
- `border-color` rule changed from `hsl(var(--border))` to `var(--border)` (CSS-04)
- `.sd-root-modern` block preserved exactly as-is (SJS-04 preservation)

**2. Replace `app/tailwind.config.js` entirely with:**

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        border: 'var(--border)',
        input: 'var(--input)',
        ring: 'var(--ring)',
        background: 'var(--background)',
        foreground: 'var(--foreground)',
        primary: {
          DEFAULT: 'var(--primary)',
          foreground: 'var(--primary-foreground)',
        },
        secondary: {
          DEFAULT: 'var(--secondary)',
          foreground: 'var(--secondary-foreground)',
        },
        destructive: {
          DEFAULT: 'var(--destructive)',
          foreground: 'var(--destructive-foreground)',
        },
        muted: {
          DEFAULT: 'var(--muted)',
          foreground: 'var(--muted-foreground)',
        },
        accent: {
          DEFAULT: 'var(--accent)',
          foreground: 'var(--accent-foreground)',
        },
        popover: {
          DEFAULT: 'var(--popover)',
          foreground: 'var(--popover-foreground)',
        },
        card: {
          DEFAULT: 'var(--card)',
          foreground: 'var(--card-foreground)',
        },
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
};
```

Key changes from original:

- All `hsl(var(--xxx))` replaced with `var(--xxx)` (CSS-02)
- Static `primary-50`, `primary-100`, `primary-500`, `primary-600`, `primary-700` entries REMOVED (CSS-01/03 prerequisite)
- `borderRadius` and `plugins` unchanged

**Do NOT commit yet. Proceed to Task 2.**
</action>
<verify>
<automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst && grep -c "hsl(var(--" app/src/index.css app/tailwind.config.js | grep -v ":0$" | wc -l</automated>
<manual>Should output 0 — no files contain hsl(var(-- pattern</manual>
</verify>
<done> - `app/src/index.css` contains `:root` block with hex values and `[data-theme='glass']` block with Glass Purple values - `app/tailwind.config.js` uses `var(--xxx)` for all color tokens with no `hsl()` wrappers - Static `primary-50/100/500/600/700` entries removed from tailwind config - `border-color: var(--border)` in the global rule (not `hsl(var(--border))`) - `.sd-root-modern` block preserved exactly - Changes are NOT yet committed
</done>
</task>

<task type="auto">
  <name>Task 2: Replace all primary-NNN class references and commit atomically</name>
  <files>
    app/src/components/AdminLayout.tsx,
    app/src/components/BugReportButton.tsx,
    app/src/components/BugReportDialog.tsx,
    app/src/components/StatusBadge.tsx,
    app/src/components/ToggleSwitch.tsx,
    app/src/pages/Dashboard.tsx,
    app/src/pages/Welcome.tsx,
    app/src/pages/admin/AssessmentCreate.tsx,
    app/src/pages/admin/AssessmentDetail.tsx,
    app/src/pages/admin/AssessmentList.tsx,
    app/src/pages/admin/InstructorList.tsx,
    app/src/pages/admin/QrPresenter.tsx,
    app/src/pages/admin/QuestionBankCreate.tsx,
    app/src/pages/admin/QuestionBankDetail.tsx,
    app/src/pages/admin/QuestionBankList.tsx,
    app/src/pages/admin/SessionDetail.tsx,
    app/src/pages/admin/SessionList.tsx,
    app/src/pages/admin/assessment-detail/EditAssessmentDialog.tsx,
    app/src/pages/admin/assessment-detail/QrCodeTab.tsx,
    app/src/pages/public/take-assessment/StudentInfoStep.tsx
  </files>
  <action>
**Replace all `primary-NNN` utility class references with semantic equivalents across all 20 component files. Then commit ALL changes (Task 1 + Task 2) as a single atomic commit.**

Use these exact replacement rules (apply as find-and-replace within className strings):

| Find                                                      | Replace With                                                |
| --------------------------------------------------------- | ----------------------------------------------------------- |
| `bg-primary-500`                                          | `bg-primary`                                                |
| `text-primary-500`                                        | `text-primary`                                              |
| `border-primary-500`                                      | `border-primary`                                            |
| `hover:bg-primary-500/90`                                 | `hover:bg-primary/90`                                       |
| `hover:bg-primary-500`                                    | `hover:bg-primary`                                          |
| `hover:border-primary-500/50`                             | `hover:border-primary/50`                                   |
| `bg-primary-500/10`                                       | `bg-primary/10`                                             |
| `peer-checked:bg-primary-500`                             | `peer-checked:bg-primary`                                   |
| `border-primary-500` (in `border-b-2 border-primary-500`) | `border-primary`                                            |
| `primary-100` (any usage)                                 | Replace contextually — likely `bg-primary/10` or `bg-muted` |
| `primary-50` (any usage)                                  | Replace contextually — likely `bg-primary/5` or `bg-muted`  |
| `primary-600` (any usage)                                 | `primary` (the DEFAULT is the semantic token)               |
| `primary-700` (any usage)                                 | `primary` (the DEFAULT is the semantic token)               |

**File-by-file specifics (from research):**

1. **AdminLayout.tsx** (7 occurrences): `border-primary-500` -> `border-primary`, `text-primary-500` -> `text-primary` (active nav indicator and text)
2. **AssessmentCreate.tsx** (5): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
3. **QuestionBankDetail.tsx** (5): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
4. **Welcome.tsx** (4): `text-primary-500` -> `text-primary`, `hover:border-primary-500/50` -> `hover:border-primary/50`
5. **AssessmentDetail.tsx** (2): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
6. **AssessmentList.tsx** (2): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
7. **QuestionBankList.tsx** (2): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
8. **SessionList.tsx** (2): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
9. **SessionDetail.tsx** (2): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
10. **InstructorList.tsx** (2): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
11. **Dashboard.tsx** (2): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
12. **QrPresenter.tsx** (2): `text-primary-500` -> `text-primary`, `bg-primary-500/10` -> `bg-primary/10`
13. **BugReportButton.tsx** (1): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
14. **BugReportDialog.tsx** (1): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
15. **StatusBadge.tsx** (1): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500` -> `hover:bg-primary`
16. **ToggleSwitch.tsx** (1): `peer-checked:bg-primary-500` -> `peer-checked:bg-primary`
17. **QuestionBankCreate.tsx** (1): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
18. **StudentInfoStep.tsx** (1): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
19. **EditAssessmentDialog.tsx** (1): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`
20. **QrCodeTab.tsx** (1): `bg-primary-500` -> `bg-primary`, `hover:bg-primary-500/90` -> `hover:bg-primary/90`

**Important notes:**

- Do NOT modify any files in `app/src/components/ui/` (shadcn primitives already use semantic tokens)
- The opacity modifiers (`/90`, `/10`, `/50`) will silently produce no CSS output in Tailwind v3 with hex vars — this is accepted behavior matching the portal
- Read each file before editing to find the exact string context for replacement

**After ALL replacements are complete:**

1. Run verification grep to confirm zero remaining `primary-500|primary-100|primary-50|primary-600|primary-700` references
2. Run `cd app && npm run build` to confirm build passes
3. Stage ALL 22 changed files and commit with message:

   ```
   feat(01-css): atomic CSS migration — HSL to hex, var() pattern, Glass Purple vars

   - Convert all :root CSS variables from HSL-component to raw hex (CSS-06)
   - Add [data-theme='glass'] block with Glass Purple palette (CSS-05)
   - Switch tailwind.config.js from hsl(var(--xxx)) to var(--xxx) (CSS-02)
   - Remove static primary-50/100/500/600/700 shade entries (CSS-01)
   - Replace 47 primary-NNN class references across 20 files (CSS-03)
   - Migrate border-color from hsl(var(--border)) to var(--border) (CSS-04)

   Known: opacity modifiers (bg-primary/90, bg-primary/10) silently no-op
   with hex CSS vars in Tailwind v3. Matches portal behavior. Phase 4
   restores hover effects via Glass Purple-specific CSS.
   ```

     </action>
     <verify>
       <automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst && grep -rn "primary-500\|primary-100\|primary-50\b\|primary-600\|primary-700" app/src/ --include="*.tsx" --include="*.ts" --include="*.css" --include="*.js" | grep -v node_modules | grep -v "tailwind.config" | wc -l && cd app && npm run build 2>&1 | tail -3</automated>
       <manual>First number should be 0 (no remaining primary-NNN refs). Build should show success.</manual>
     </verify>
     <done>
       - Zero search results for `primary-500`, `primary-100`, `primary-50`, `primary-600`, `primary-700` across all source files
       - Zero search results for `hsl(var(--` across `index.css` and `tailwind.config.js`
       - `npm run build` passes with zero errors
       - `npm run typecheck` passes with zero errors
       - All 22 files committed in a single atomic commit
       - `.sd-root-modern` border-color revert block preserved unchanged
       - `[data-theme='glass']` block present in `index.css` with Glass Purple values
     </done>
   </task>

</tasks>

<verification>
```bash
# CSS-01/02: No hsl(var( anywhere in CSS or config
grep -rn "hsl(var(--" app/src/index.css app/tailwind.config.js
# Expected: no output

# CSS-03: No primary-NNN classes anywhere in source

grep -rn "primary-500\|primary-100\|primary-50\b\|primary-600\|primary-700" app/src/ --include="_.tsx" --include="_.ts" --include="\*.css"

# Expected: no output

# CSS-04: border-color uses var() not hsl()

grep "border-color" app/src/index.css

# Expected: border-color: var(--border) and border-color: revert (SurveyJS block)

# CSS-05: Glass Purple variables exist

grep "data-theme.\*glass" app/src/index.css

# Expected: [data-theme='glass'] {

# CSS-06: Primary is hex

grep "\-\-primary:" app/src/index.css

# Expected: --primary: #1b5fd0 (under :root) and --primary: #8b5cf6 (under glass)

# Build passes

cd app && npm run build

# Typecheck passes

cd app && npm run typecheck

```
</verification>

<success_criteria>
- The app renders visually identical to the pre-migration state in the browser (no visual regression in Daylight)
- `var(--primary)` resolves to `#1b5fd0` in DevTools with no `hsl()` wrapper
- `grep -rn "hsl(var(--" app/src/ app/tailwind.config.js` returns zero results
- `grep -rn "primary-500\|primary-100\|primary-50\b\|primary-600\|primary-700" app/src/` returns zero results
- `npm run build` passes with zero errors
- `npm run typecheck` passes with zero errors
- All changes are in a single atomic git commit (22 files)
- `[data-theme='glass']` selector is defined with Glass Purple CSS variables in `index.css`
</success_criteria>

<output>
After completion, create `.planning/phases/01-atomic-css-migration/01-02-SUMMARY.md`
</output>
```
