---
phase: 04-glass-purple-visual-effects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ui/sheet.tsx
  - app/src/components/ui/tooltip.tsx
  - app/src/components/ui/scroll-area.tsx
  - app/src/components/ui/popover.tsx
  - app/src/components/ui/checkbox.tsx
  - app/src/components/ui/switch.tsx
  - app/tsconfig.json
  - app/tsconfig.app.json
autonomous: true
requirements:
  - CMP-03

must_haves:
  truths:
    - 'Six new shadcn/ui components (Sheet, Tooltip, ScrollArea, Popover, Checkbox, Switch) are installed and importable'
    - '`npm run build` and `npm run typecheck` pass with zero errors'
  artifacts:
    - path: 'app/src/components/ui/sheet.tsx'
      provides: 'Sheet component (mobile sidebar drawer)'
      contains: 'SheetContent'
    - path: 'app/src/components/ui/tooltip.tsx'
      provides: 'Tooltip component'
      contains: 'TooltipContent'
    - path: 'app/src/components/ui/scroll-area.tsx'
      provides: 'ScrollArea component'
      contains: 'ScrollArea'
    - path: 'app/src/components/ui/popover.tsx'
      provides: 'Popover component'
      contains: 'PopoverContent'
    - path: 'app/src/components/ui/checkbox.tsx'
      provides: 'Checkbox component'
      contains: 'Checkbox'
    - path: 'app/src/components/ui/switch.tsx'
      provides: 'Switch component'
      contains: 'Switch'
  key_links:
    - from: 'app/src/components/ui/sheet.tsx'
      to: '@radix-ui/react-dialog'
      via: 'import'
      pattern: 'radix-ui.*dialog'
    - from: 'app/src/components/ui/tooltip.tsx'
      to: '@radix-ui/react-tooltip'
      via: 'import'
      pattern: 'radix-ui.*tooltip'
---

<objective>
Install the six new shadcn/ui components required by Phase 4 and establish a clean build baseline so that subsequent plans can verify via `npm run build`.

Purpose: CMP-03 requires Sheet, Tooltip, ScrollArea, Popover, Checkbox, and Switch to be installed and usable. Additionally, the research identified pre-existing TypeScript build errors in test files that may block the `npm run build` / `npm run typecheck` success gate. This plan ensures a clean foundation before applying visual effects.

Output: Six new component files in `app/src/components/ui/`, a passing build, and a passing typecheck.
</objective>

<execution_context>
@/home/jeramey/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeramey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-glass-purple-visual-effects/04-RESEARCH.md
@app/components.json
@app/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install six shadcn/ui components via CLI</name>
  <files>
    app/src/components/ui/sheet.tsx
    app/src/components/ui/tooltip.tsx
    app/src/components/ui/scroll-area.tsx
    app/src/components/ui/popover.tsx
    app/src/components/ui/checkbox.tsx
    app/src/components/ui/switch.tsx
    app/package.json
  </files>
  <action>
Run the shadcn CLI from the `app/` directory to install all six components in a single command:

```bash
cd /path/to/project/app
npx shadcn@latest add sheet tooltip scroll-area popover checkbox switch --yes
```

The CLI reads `app/components.json` (style: "new-york", tsx: true, cssVariables: true) and scaffolds component files into `app/src/components/ui/`. It also installs required Radix peer dependencies automatically:

- Sheet uses `@radix-ui/react-dialog` (already installed)
- Tooltip installs `@radix-ui/react-tooltip`
- ScrollArea installs `@radix-ui/react-scroll-area`
- Popover installs `@radix-ui/react-popover`
- Checkbox installs `@radix-ui/react-checkbox`
- Switch installs `@radix-ui/react-switch`

After installation, verify that all six files exist and all six named exports resolve:

```bash
ls app/src/components/ui/{sheet,tooltip,scroll-area,popover,checkbox,switch}.tsx
```

Do NOT modify the generated component files. They should work out of the box with the existing Tailwind config and `cn()` utility.
</action>
<verify>
<automated>ls app/src/components/ui/sheet.tsx app/src/components/ui/tooltip.tsx app/src/components/ui/scroll-area.tsx app/src/components/ui/popover.tsx app/src/components/ui/checkbox.tsx app/src/components/ui/switch.tsx && echo "All 6 components present"</automated>
<manual>Verify each file contains expected exports (Sheet, Tooltip, ScrollArea, Popover, Checkbox, Switch)</manual>
</verify>
<done>All six component files exist in app/src/components/ui/ and their Radix peer dependencies are installed in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Fix pre-existing TypeScript build errors and verify clean build</name>
  <files>
    app/tsconfig.json
    app/tsconfig.app.json
  </files>
  <action>
The research identified pre-existing TypeScript errors in `TakeAssessment.test.tsx` (missing `@testing-library/react` types and `toBeInTheDocument` assertion errors) that cause `npm run typecheck` and `npm run build` to fail.

**Step 1: Check if the errors still exist after Phases 1-3.**

Run `cd app && npm run typecheck` to see the current state. If it passes cleanly, skip to Step 3.

**Step 2: If errors exist, fix the TypeScript configuration.**

The cleanest fix is to ensure test files are excluded from the production build's type check. Check if `app/tsconfig.app.json` already exists. If not, look at how the build script runs TypeScript — if it runs `tsc --noEmit` against the default `tsconfig.json` which includes test files, then either:

(a) Create `app/tsconfig.app.json` that extends `app/tsconfig.json` but adds `"exclude": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/**/__tests__/**"]` and update the build/typecheck scripts to use `tsc --noEmit -p tsconfig.app.json`, OR

(b) If `@testing-library/jest-dom` types need to be referenced, add the proper type reference in `app/src/vite-env.d.ts` or a `setupTests.ts` file (e.g., `/// <reference types="@testing-library/jest-dom" />`).

Choose the approach that achieves `npm run build` and `npm run typecheck` passing with zero errors while preserving test file functionality for `vitest`.

**Step 3: Verify the complete build pipeline passes.**

```bash
cd app && npm run typecheck && npm run build
```

Both commands must exit with code 0. If new TypeScript errors were introduced by the shadcn component installation (unlikely but possible), fix those as well.

**Important:** Do NOT change any non-config/non-test files. The goal is solely to establish a clean build baseline. Do NOT modify source component files, CSS files, or page files.
</action>
<verify>
<automated>cd app && npm run typecheck && npm run build</automated>
</verify>
<done>`npm run typecheck` exits 0 and `npm run build` exits 0 with no errors. The six new shadcn components compile without issues.</done>
</task>

</tasks>

<verification>
1. All six component files exist: `ls app/src/components/ui/{sheet,tooltip,scroll-area,popover,checkbox,switch}.tsx`
2. `cd app && npm run typecheck` passes with exit code 0
3. `cd app && npm run build` passes with exit code 0
4. The following imports resolve without error (verified by successful typecheck):
   - `import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetTrigger } from '@/components/ui/sheet'`
   - `import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip'`
   - `import { ScrollArea } from '@/components/ui/scroll-area'`
   - `import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover'`
   - `import { Checkbox } from '@/components/ui/checkbox'`
   - `import { Switch } from '@/components/ui/switch'`
</verification>

<success_criteria>

- Six new shadcn/ui component files installed in app/src/components/ui/
- All Radix peer dependencies installed
- `npm run typecheck` passes clean
- `npm run build` produces dist/ output with no errors
  </success_criteria>

<output>
After completion, create `.planning/phases/04-glass-purple-visual-effects/04-01-SUMMARY.md`
</output>
