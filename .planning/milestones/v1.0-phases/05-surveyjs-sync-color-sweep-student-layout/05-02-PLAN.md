---
phase: 05-surveyjs-sync-color-sweep-student-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/StudentLayout.tsx
autonomous: true
requirements:
  - STU-01
  - STU-02
  - STU-03
  - STU-04

must_haves:
  truths:
    - 'Student pages display Glass Purple background effects when Glass Purple theme is active'
    - 'A sun/moon theme toggle is visible in the student header'
    - 'Theme preference set on the admin side carries over to the student side on page load'
  artifacts:
    - path: 'app/src/components/StudentLayout.tsx'
      provides: 'Theme-integrated student layout with toggle and glass effects'
      contains: 'useTheme'
  key_links:
    - from: 'app/src/components/StudentLayout.tsx'
      to: 'ThemeContext'
      via: 'useTheme() hook'
      pattern: "useTheme\\(\\)"
    - from: 'app/src/components/StudentLayout.tsx'
      to: 'ThemeToggle component'
      via: 'JSX import and render'
      pattern: '<ThemeToggle'
---

<objective>
Upgrade StudentLayout to consume ThemeContext, apply Glass Purple background effects on student pages, and add the sun/moon theme toggle to the student header.

Purpose: Student pages currently ignore the active theme — they always render light mode regardless of the user's preference, breaking the theme consistency promise.
Output: StudentLayout.tsx with ThemeContext integration, Glass Purple body gradient, and ThemeToggle in the header.
</objective>

<execution_context>
@/home/jeramey/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeramey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-RESEARCH.md
@app/src/components/StudentLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ThemeContext and Glass Purple effects into StudentLayout</name>
  <files>app/src/components/StudentLayout.tsx</files>
  <action>
1. Add imports at the top of StudentLayout.tsx:
   - `import { useTheme } from '@/contexts/ThemeContext';` (ThemeContext built in Phase 2)
   - Import the ThemeToggle component built in Phase 2. Check the actual export location — likely `@/components/ThemeToggle` or similar. If unsure, grep for `ThemeToggle` in app/src/components/ to find the exact path.

2. Inside the StudentLayout component function, call `const { theme } = useTheme();`

3. On the outermost `<div>` that wraps the entire student layout:
   - Ensure it has `className="min-h-screen bg-background"` (should already have `bg-background` from semantic tokens)
   - The Glass Purple body gradient is controlled by the `[data-theme="glass"]` CSS variables defined in index.css (Phase 1/2). The `bg-background` token automatically resolves to the correct color for each theme. If the admin AppShell (Phase 3) applies an additional radial gradient class, apply the same pattern here. Check Phase 3/4 implementation: if AppShell uses a utility class like `glass-gradient` or inline style for the body background gradient, apply the same approach. The simplest approach: the `bg-background` CSS variable under `[data-theme="glass"]` should already include the dark background. If a radial gradient overlay is needed, add it conditionally:

   ```tsx
   <div className={cn('min-h-screen bg-background', theme === 'glass' && 'glass-body-gradient')}>
   ```

   where `glass-body-gradient` is a utility class defined in Phase 3/4's CSS. If no such class exists, the dark `bg-background` value alone is sufficient.

4. In the `<header>` element:
   - Replace any `bg-white` with `bg-card` (header surface should use card token for glass translucency)
   - Ensure border uses `border-border` (likely already semantic)
   - Add the ThemeToggle component in the header's right-side controls area, next to the existing user name and logout button:

   ```tsx
   <div className="flex items-center gap-3">
     {/* existing user name and logout */}
     <ThemeToggle />
   </div>
   ```

5. STU-04 (shared localStorage key): No code change needed — ThemeContext from Phase 2 reads/writes a single localStorage key shared by both admin and student sides. Verify by checking that `useTheme()` uses the same storage key regardless of which layout renders it.

AVOID:

- Wrapping StudentLayout in a NEW ThemeContext.Provider — the provider should already be at the App level (Phase 2)
- Adding a separate localStorage read for theme — use useTheme() only
- Replacing intentional bg-white if any QR code elements exist in student pages (they don't — QR is admin-only)
  </action>
  <verify>
  <automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst/app && npx tsc --noEmit 2>&1 | head -30</automated>
  <manual>In browser: navigate to a student page in Glass Purple — verify dark background and toggle in header. Switch theme via toggle — verify it changes. Refresh page — verify theme persists.</manual>
  </verify>
  <done>StudentLayout.tsx imports useTheme() and ThemeToggle, applies Glass Purple background effects via semantic bg-background token (and optional gradient class), and renders ThemeToggle in the student header. Theme preference persists across admin and student sides via shared localStorage key. TypeScript compiles clean.</done>
  </task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes with zero errors
- `grep -n "useTheme" app/src/components/StudentLayout.tsx` shows the hook call
- `grep -n "ThemeToggle" app/src/components/StudentLayout.tsx` shows the toggle in JSX
- `grep -c "bg-white" app/src/components/StudentLayout.tsx` returns 0 (no hardcoded white in layout contexts)
</verification>

<success_criteria>

- StudentLayout reads theme from ThemeContext
- Glass Purple background effects visible on student pages
- Sun/moon toggle present in student header
- Theme preference carries over from admin side (shared localStorage)
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-02-SUMMARY.md`
</output>
