---
phase: 05-surveyjs-sync-color-sweep-student-layout
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - app/src/components/StatusBadge.tsx
  - app/src/components/BugReportButton.tsx
  - app/src/components/BugReportDialog.tsx
  - app/src/components/ToggleSwitch.tsx
  - app/src/components/AdminLayout.tsx
  - app/src/pages/Login.tsx
  - app/src/pages/Welcome.tsx
  - app/src/pages/NotFound.tsx
  - app/src/pages/Dashboard.tsx
  - app/src/pages/public/TakeAssessment.tsx
  - app/src/pages/public/AttendSession.tsx
  - app/src/pages/public/CheckOutSession.tsx
  - app/src/pages/public/CreateAccount.tsx
  - app/src/pages/public/take-assessment/StudentInfoStep.tsx
  - app/src/pages/public/take-assessment/AssessmentResults.tsx
autonomous: true
requirements:
  - CLR-01
  - CLR-02
  - CLR-03
  - CLR-04
  - CLR-05
  - CLR-06
  - CLR-07
  - CLR-08

must_haves:
  truths:
    - 'StatusBadge, BugReportButton, BugReportDialog, and ToggleSwitch render correctly in both Daylight and Glass Purple'
    - 'All public pages (Login, Welcome, TakeAssessment, AttendSession, CheckOutSession, CreateAccount) use semantic color tokens instead of hardcoded grays'
    - 'Intentional whites on QR code containers and AssessmentResults score circle are preserved with comments'
    - 'ToggleSwitch thumb remains white (intentional) while wrapper uses bg-card'
  artifacts:
    - path: 'app/src/components/StatusBadge.tsx'
      provides: 'Theme-aware status badge'
      contains: 'bg-primary'
    - path: 'app/src/components/ToggleSwitch.tsx'
      provides: 'Theme-aware toggle with intentional white thumb'
      contains: 'bg-card'
    - path: 'app/src/components/BugReportButton.tsx'
      provides: 'Theme-aware bug report button'
      contains: 'bg-primary'
  key_links:
    - from: 'app/src/components/StatusBadge.tsx'
      to: 'Tailwind semantic tokens'
      via: 'className strings'
      pattern: 'bg-primary text-primary-foreground'
    - from: 'app/src/components/ToggleSwitch.tsx'
      to: 'Tailwind semantic tokens'
      via: 'className strings'
      pattern: 'bg-card.*peer-checked:bg-primary'
---

<objective>
Sweep all components and public/shared pages to replace hardcoded gray, white, slate, and primary-500 classes with semantic Tailwind tokens, while preserving intentional whites.

Purpose: Components and shared pages currently use hardcoded color classes (bg-white, bg-gray-50, text-gray-600, bg-primary-500) that break in Glass Purple theme. Semantic tokens (bg-background, text-muted-foreground, bg-primary) adapt automatically.
Output: 15 files with all hardcoded color references replaced by semantic equivalents. Intentional whites preserved with code comments.
</objective>

<execution_context>
@/home/jeramey/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeramey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color sweep - components (StatusBadge, BugReport*, ToggleSwitch, AdminLayout)</name>
  <files>
    app/src/components/StatusBadge.tsx
    app/src/components/BugReportButton.tsx
    app/src/components/BugReportDialog.tsx
    app/src/components/ToggleSwitch.tsx
    app/src/components/AdminLayout.tsx
  </files>
  <action>
Apply the semantic color replacement map from research. For each file:

**StatusBadge.tsx (CLR-05):**

- `bg-primary-500 text-white hover:bg-primary-500` -> `bg-primary text-primary-foreground hover:bg-primary/90`
- Any other hardcoded gray/white classes -> semantic equivalents per research map

**BugReportButton.tsx (CLR-06):**

- `bg-primary-500` -> `bg-primary`
- `hover:bg-primary-500/90` -> `hover:bg-primary/90`
- Any `text-white` on primary buttons -> `text-primary-foreground`

**BugReportDialog.tsx (CLR-06):**

- `text-gray-500` -> `text-muted-foreground`
- `bg-primary-500` -> `bg-primary`
- `hover:bg-primary-500/90` -> `hover:bg-primary/90`
- Any `text-white` on primary buttons -> `text-primary-foreground`

**ToggleSwitch.tsx (CLR-07):**

- Wrapper `bg-white` -> `bg-card`
- Track `peer-checked:bg-primary-500` -> `peer-checked:bg-primary`
- Thumb `after:bg-white` -> PRESERVE (intentional white toggle thumb). Add comment: `/* intentional: toggle thumb must remain white for contrast */`

**AdminLayout.tsx:**

- `bg-gray-50` -> `bg-background`
- `bg-white` (header) -> `bg-card`
- `border-primary-500` -> `border-primary`
- `text-primary-500` -> `text-primary`
- All other `primary-500` references -> `primary` (remove numeric suffix)
- NOTE: AdminLayout may be superseded by AppShell from Phase 3, but sweep it anyway to pass the grep check (CLR-09)

AVOID:

- Replacing `text-white` that is used as foreground on primary/colored buttons — use `text-primary-foreground` instead
- Removing ANY classes, only replacing color values
- Touching non-color classes (padding, margin, flex, etc.)
  </action>
  <verify>
  <automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst && grep -rn "bg-primary-500\|primary-500\|bg-gray-\|text-gray-\|border-gray-\|bg-white" app/src/components/StatusBadge.tsx app/src/components/BugReportButton.tsx app/src/components/BugReportDialog.tsx app/src/components/ToggleSwitch.tsx app/src/components/AdminLayout.tsx 2>/dev/null | grep -v "intentional" | head -20</automated>
  <manual>Verify no hardcoded grays/whites remain except intentional-marked lines</manual>
  </verify>
  <done>All 5 component files use semantic Tailwind tokens. StatusBadge uses bg-primary/text-primary-foreground. BugReport\* uses bg-primary and text-muted-foreground. ToggleSwitch wrapper uses bg-card with intentional white thumb preserved. AdminLayout uses semantic tokens throughout.</done>
  </task>

<task type="auto">
  <name>Task 2: Color sweep - top-level pages and public pages</name>
  <files>
    app/src/pages/Login.tsx
    app/src/pages/Welcome.tsx
    app/src/pages/NotFound.tsx
    app/src/pages/Dashboard.tsx
    app/src/pages/public/TakeAssessment.tsx
    app/src/pages/public/AttendSession.tsx
    app/src/pages/public/CheckOutSession.tsx
    app/src/pages/public/CreateAccount.tsx
    app/src/pages/public/take-assessment/StudentInfoStep.tsx
    app/src/pages/public/take-assessment/AssessmentResults.tsx
  </files>
  <action>
Apply the semantic color replacement map from research. For each file:

**Login.tsx (CLR-02):**

- `bg-gray-50` -> `bg-background`

**Welcome.tsx (CLR-01, CLR-02, CLR-03):**

- `bg-gray-50` -> `bg-background`
- `text-gray-900` -> `text-foreground`
- `text-gray-600` -> `text-muted-foreground`
- `bg-gray-100` -> `bg-muted`
- `bg-gray-200` -> `bg-muted`
- `bg-blue-100` -> `bg-primary/10` (this is a feature stat card, not a semantic status color)
- `text-primary-500` -> `text-primary`
- `bg-primary-500` -> `bg-primary`

**NotFound.tsx (CLR-03):**

- `text-gray-500` -> `text-muted-foreground`
- `text-gray-600` -> `text-muted-foreground`

**Dashboard.tsx:**

- `text-primary-500` -> `text-primary`
- `bg-primary-500` -> `bg-primary`

**TakeAssessment.tsx (CLR-03):**

- `text-slate-900` -> `text-foreground`
- `text-slate-600` -> `text-muted-foreground`
- `text-gray-600` -> `text-muted-foreground`
- NOTE: Plan 01 already added SurveyJS theme code. This task only replaces remaining hardcoded text colors in the non-SurveyJS parts of the component (status messages, headings, etc.)

**AttendSession.tsx (CLR-02, CLR-03):**

- `bg-gray-50` -> `bg-background`
- `text-gray-500` -> `text-muted-foreground`
- `text-gray-600` -> `text-muted-foreground`

**CheckOutSession.tsx (CLR-02, CLR-03):**

- `bg-gray-50` -> `bg-background`
- `text-gray-500` -> `text-muted-foreground`
- `text-gray-600` -> `text-muted-foreground`

**CreateAccount.tsx (CLR-03):**

- `text-gray-600` -> `text-muted-foreground`

**StudentInfoStep.tsx (CLR-01):**

- `bg-white` (stat boxes) -> `bg-card`
- `bg-primary-500` -> `bg-primary`

**AssessmentResults.tsx (CLR-01, CLR-03, CLR-08):**

- `text-slate-700` -> `text-muted-foreground`
- `text-gray-600` -> `text-muted-foreground`
- `bg-white` on score circle -> PRESERVE. Add comment: `/* intentional: score circle visual design */`
- Any `text-white` on primary buttons -> `text-primary-foreground` if on a `bg-primary` button, otherwise preserve

AVOID:

- Replacing `bg-blue-50`, `bg-amber-50`, `bg-emerald-50`, `bg-emerald-600` — these are intentional semantic status colors NOT in the sweep pattern
- Replacing `bg-red-100`, `bg-purple-100`, `bg-orange-500` — intentional category/severity colors
- Replacing `bg-white` on QR code containers — these are handled in the QR-specific files in Plan 04
  </action>
  <verify>
  <automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst && grep -rn "bg-primary-500\|primary-50[^0]\|primary-100\|primary-500\|bg-gray-\|text-gray-\|border-gray-\|text-slate-\|border-slate-" app/src/pages/Login.tsx app/src/pages/Welcome.tsx app/src/pages/NotFound.tsx app/src/pages/Dashboard.tsx app/src/pages/public/TakeAssessment.tsx app/src/pages/public/AttendSession.tsx app/src/pages/public/CheckOutSession.tsx app/src/pages/public/CreateAccount.tsx app/src/pages/public/take-assessment/StudentInfoStep.tsx app/src/pages/public/take-assessment/AssessmentResults.tsx 2>/dev/null | head -20</automated>
  <manual>Verify no hardcoded grays/slates remain in listed files</manual>
  </verify>
  <done>All 10 page files use semantic Tailwind tokens. No bg-gray-_, text-gray-_, text-slate-_, border-slate-_, or primary-500 references remain. AssessmentResults score circle bg-white preserved with intentional comment. Intentional status colors (blue, amber, emerald) untouched.</done>
  </task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes with zero errors
- Combined grep across all 15 files returns zero forbidden pattern matches (excluding intentional-marked lines)
- StatusBadge, BugReport*, ToggleSwitch all use semantic tokens
- AssessmentResults score circle bg-white has intentional comment
- ToggleSwitch thumb after:bg-white has intentional comment
</verification>

<success_criteria>

- All 15 files migrated to semantic Tailwind tokens
- Zero remaining hardcoded gray/white/slate/primary-500 classes (except intentional)
- Intentional whites preserved with code comments (ToggleSwitch thumb, AssessmentResults score circle)
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/05-surveyjs-sync-color-sweep-student-layout/05-03-SUMMARY.md`
</output>
