---
phase: 01-atomic-css-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package-lock.json
autonomous: true
requirements:
  - CSS-01
must_haves:
  truths:
    - '`cd app && npm run build` completes with zero errors'
    - '`cd app && npm run typecheck` completes with zero errors'
  artifacts:
    - path: 'app/node_modules/@testing-library'
      provides: 'Installed test dependencies that currently block build'
  key_links:
    - from: 'app/tsconfig.json'
      to: 'app/src/__tests__/'
      via: 'TypeScript include/exclude configuration'
      pattern: 'include.*src'
---

<objective>
Fix pre-existing build blocker so that `npm run build` and `npm run typecheck` pass cleanly before the CSS migration begins.

Purpose: The atomic CSS migration (Plan 02) requires `npm run build` to pass as its verification step. Currently the build fails with 31 TypeScript errors because `@testing-library` packages are declared in `package.json` but missing from `node_modules`. This plan installs them and resolves any remaining test-file TypeScript issues so the build baseline is green.

Output: A passing `npm run build` and `npm run typecheck` in the `app/` directory.
</objective>

<execution_context>
@/home/jeramey/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeramey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-atomic-css-migration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install missing dependencies and fix build errors</name>
  <files>app/package-lock.json</files>
  <action>
1. Run `cd app && npm install` to install the missing `@testing-library/react`, `@testing-library/user-event`, and `@testing-library/jest-dom` packages that are already listed in `package.json` but absent from `node_modules`.

2. Run `cd app && npm run build` to check if the build passes after install.

3. If test file TypeScript errors persist (likely `@testing-library/jest-dom` type augmentation issues):
   - Read `app/tsconfig.json` and `app/tsconfig.app.json` (if it exists)
   - Option A (preferred): If a `tsconfig.app.json` exists that excludes tests and is used by the build, no further action needed — the build should already pass since Vite typically uses `tsconfig.app.json`.
   - Option B: If the build config includes test files, add `"src/__tests__"` to the `exclude` array in the tsconfig used by the build (usually `tsconfig.app.json`).
   - Option C: If neither works, add `"@testing-library/jest-dom"` to the `types` array in `tsconfig.json`.

4. Run `cd app && npm run build` again to confirm zero errors.
5. Run `cd app && npm run typecheck` to confirm zero errors.

Do NOT modify any CSS, Tailwind config, or component files in this task. This is purely a build infrastructure fix.
</action>
<verify>
<automated>cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst/app && npm run build 2>&1 | tail -5</automated>
<manual>Build output shows successful completion with no TypeScript errors</manual>
</verify>
<done> - `cd app && npm run build` exits with code 0 - `cd app && npm run typecheck` exits with code 0 - No source code changes to CSS or component files — only dependency installation and possibly tsconfig adjustment
</done>
</task>

</tasks>

<verification>
```bash
# Full build passes
cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst/app && npm run build

# TypeScript check passes

cd /home/jeramey/projects/mededprep-ecosystem/mededprep-inst/app && npm run typecheck

# No CSS or component files modified (only package-lock.json and possibly tsconfig)

git diff --name-only

```
</verification>

<success_criteria>
- `npm run build` in `app/` exits with code 0 and produces `dist/` output
- `npm run typecheck` in `app/` exits with code 0
- The app renders identically in the browser (no changes to source code)
</success_criteria>

<output>
After completion, create `.planning/phases/01-atomic-css-migration/01-01-SUMMARY.md`
</output>
```
